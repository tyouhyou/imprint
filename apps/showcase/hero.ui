column spacing=6 padding=8
  panel id="chart_slot"
  label text="DEVICE STATUS"
  row spacing=6
    label text="CPU"
    progress_bar id="cpu_bar"
  row spacing=6
    label text="MEMORY"
    progress_bar id="mem_bar"
  row spacing=6
    label text="TEMP"
    progress_bar id="temp_bar"
  row spacing=6
    label text="STATE"
    label id="state_value" text="IDLE"
  row spacing=6
    button id="start_btn" text="START"
    button id="stop_btn" text="STOP"
    button id="replay_btn" text="REPLAY"
  row spacing=6
    button id="theme_btn" text="DARK"
    button id="gallery_btn" text="GALLERY"
